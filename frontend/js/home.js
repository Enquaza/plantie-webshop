function goToCategory(category) {
    window.location.href = `/sites/products.html?category=${category}`;
}

function goToLevel(level) {
    window.location.href = `/sites/products.html?level=${level}`;
}